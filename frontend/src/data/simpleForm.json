{
    "type": "object",
    "properties": {
      "title": {
        "title": "Title",
        "type": "string",
        "minLength": 10,
        "maxLength": 140
      },
      "slug": {
        "title": "Slug",
        "type": "string",
        "pattern": "^[a-z0-9-]+$"
      },
      "published": {
        "title": "Published",
        "type": "string",
        "format": "date-time"
      },
      "content": {
        "title": "Content",
        "type": "string"
      }
    },
    "required": ["title", "slug", "published", "content"]
  }